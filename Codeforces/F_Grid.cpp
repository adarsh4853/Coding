#include <algorithm>
#include <array>
#include <bitset>
#include <cassert>
#include <chrono>
#include <cmath>
#include <cstdint>
#include <cstring>
#include <functional>
#include <iomanip>
#include <iostream>
#include <map>
#include <numeric>
#include <queue>
#include <random>
#include <set>
#include <vector>
using namespace std;

void run_case(int& tttt) 
{
    //cout<<"Case"<<" : "<<tttt<<"\n";

    string s[21];
    for(int i=0;i<21;i++)
    {
        cin>>s[i];
    // int decimal = stoi(s[i], nullptr, 2);
    // cout << "Decimal: " << decimal << endl;
    }
    cout<<12;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int tests = 1;
    // cin >> tests;

    for(int i=0;i<tests;i++)
        run_case(i);
}