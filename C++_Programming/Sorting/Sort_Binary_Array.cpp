#include <bits/stdc++.h>
using namespace std;

void sortBin(vector<int> &v,int n)
{
    
}

int main()
{
    int n;
    cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++) cin>>v[i];

    sortBin(v,n);
    return 0;
}